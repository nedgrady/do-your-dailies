{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start: server (hot reload)",
      "type": "shell",
      "command": "air -c .air.toml",
      "options": {
        "cwd": "${workspaceFolder}/server"
      },
      "isBackground": true,
      "problemMatcher": []
    }
  ]
}
